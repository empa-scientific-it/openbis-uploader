
stages:
  - docker
  - test
  - build
  - deploy

build_data_discovery:
  stage: docker
  image: docker:latest
  services: 
    - docker:dind
  tags:
    - dind
  before_script:
    - echo -n $CI_REGISTRY_PASSWORD  | docker login -u $CI_REGISTRY_USER --password-stdin $CI_REGISTRY
  script:
    - docker build --pull -t "$CI_REGISTRY_IMAGE"/data-discovery:latest ./services/data-discovery/
    - docker push "$CI_REGISTRY_IMAGE"/data-discovery:latest
  only:
    changes:
      - services/data-discovery/Dockerfile
      - services/data-discovery/requirements.txt


test_data_discovery:
  stage: test
  image: alpine/curl:latest
  before_script:
    - echo -n $CI_REGISTRY_PASSWORD  | docker login -u $CI_REGISTRY_USER --password-stdin $CI_REGISTRY
  services:
    - name: data-discovery:latest
  script:
    - curl data-discovery/
    
